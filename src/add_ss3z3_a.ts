/**
 * Adds two numbers together.
 * @param a - The first number to add
 * @param b - The second number to add
 * @returns The sum of a and b
 */
export function add_ss3z3_a(a: number, b: number): number {
  return a + b;
}
