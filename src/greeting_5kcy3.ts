/**
 * Greeting utility module providing the greet_5kcy3 function.
 */

/**
 * Returns a greeting string for the given name.
 * @param name - The name to greet
 * @returns A greeting in the format "Hello, {name}!"
 */
export function greet_5kcy3(name: string): string {
  return "Hello, " + name + "!";
}
