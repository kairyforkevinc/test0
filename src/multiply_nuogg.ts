/**
 * Multiplies two numbers together.
 * @param a - The first number (multiplicand)
 * @param b - The second number (multiplier)
 * @returns The product of a and b
 */
export function multiply_nuogg(a: number, b: number): number {
  return a * b;
}
