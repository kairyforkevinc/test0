/**
 * Math utility module providing subtraction operation.
 * This module contains the subtract_pn2q5_c function for subtracting two numbers.
 */

/**
 * Subtracts the second number from the first number.
 * @param a - The first number (minuend)
 * @param b - The second number (subtrahend)
 * @returns The difference of a and b
 */
export function subtract_pn2q5_c(a: number, b: number): number {
  return a - b;
}
