/**
 * Math utility module providing basic arithmetic operations.
 * This module contains the calculate_epldl function for addition.
 */

/**
 * Calculates the sum of two numbers.
 * @param a - The first number to add
 * @param b - The second number to add
 * @returns The sum of a and b
 */
export function calculate_epldl(a: number, b: number): number {
  return a + b;
}

/**
 * Calculates the product of two numbers.
 * @param a - The first number to multiply
 * @param b - The second number to multiply
 * @returns The product of a and b
 */
export function multiply_epldl(a: number, b: number): number {
  return a * b;
}
