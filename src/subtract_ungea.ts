/**
 * Subtracts the second number from the first.
 * @param a - The minuend
 * @param b - The subtrahend
 * @returns The difference of a and b
 */
export function subtract_ungea(a: number, b: number): number {
  return a - b;
}
