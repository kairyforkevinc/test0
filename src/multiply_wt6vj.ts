/**
 * Multiplies two numbers.
 * @param a - The first factor
 * @param b - The second factor
 * @returns The product of a and b
 */
export function multiply_wt6vj(a: number, b: number): number {
  return a * b;
}
