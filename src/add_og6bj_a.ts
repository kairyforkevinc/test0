/**
 * Returns the sum of two numbers.
 * @param a - The first number
 * @param b - The second number
 * @returns The sum of a and b
 */
export function add_og6bj_a(a: number, b: number): number {
  return a + b;
}
