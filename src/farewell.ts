/**
 * Returns a farewell message for the given name.
 * @param name - The name to include in the farewell
 * @returns A farewell string in the format "Goodbye, {name}!"
 */
export function farewell(name: string): string {
  return `Goodbye, ${name}!`;
}
