/**
 * Greeting utility module.
 * @module greet_i228
 */

/**
 * Returns a greeting message for the given name.
 * @param name - The name to greet
 * @returns A greeting string in the format "Hello, {name}!"
 */
export function greet_i228(name: string): string {
  return `Hello, ${name}!`;
}
